---
---

  <div class="p-8">
    <h1 class="text-3xl mb-6 text-text-primary">DarkFlows Color Test Page</h1>
    
    <div class="space-y-4">
      <div class="p-4 bg-background border-2 border-border">
        <span class="text-text-primary">bg-background (should be pure black #000000)</span>
      </div>
      
      <div class="p-4 bg-surface border-2 border-border">
        <span class="text-text-primary">bg-surface (should be gray-900 #111827)</span>
      </div>
      
      <div class="p-4 bg-surface-hover border-2 border-border">
        <span class="text-text-primary">bg-surface-hover (should be gray-800 #1F2937)</span>
      </div>
      
      <div class="p-4 bg-primary text-text-inverse">
        bg-primary (should be red-600 #DC2626)
      </div>
      
      <div class="p-4 bg-secondary text-text-inverse">
        bg-secondary (should be orange-600 #EA580C)
      </div>
      
      <div class="p-4 bg-accent text-black">
        bg-accent (should be yellow-400 #FACC15)
      </div>
      
      <div class="p-4 border-2 border-border">
        <span class="text-text-primary">text-text-primary (should be gray-100 #F3F4F6)</span><br>
        <span class="text-text-secondary">text-text-secondary (should be gray-300 #D1D5DB)</span><br>
        <span class="text-text-muted">text-text-muted (should be gray-500 #6B7280)</span><br>
        <span class="text-link">text-link (should be red-400 #F87171)</span><br>
        <span class="text-link-hover">text-link-hover (should be red-300 #FCA5A5)</span>
      </div>
      
      <div class="p-4 bg-white text-black">
        This div has explicit bg-white to show contrast with background
      </div>
    </div>
    
    <div class="mt-8 p-4 border-2 border-border">
      <h2 class="text-xl mb-4">Debugging Info:</h2>
      <p class="text-text-secondary">If the background appears white instead of black, the CSS might not be loading correctly.</p>
      <p class="text-text-secondary">Check the browser console for any CSS loading errors.</p>
    </div>
  </div>
