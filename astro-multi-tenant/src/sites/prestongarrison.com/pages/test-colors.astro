---
---

  <div class="p-8">
    <h1 class="text-3xl mb-6 text-text-primary">Preston Garrison Color Test Page</h1>
    
    <div class="space-y-4">
      <div class="p-4 bg-background border-2 border-border">
        <span class="text-text-primary">bg-background (should be white #FFFFFF)</span>
      </div>
      
      <div class="p-4 bg-surface border-2 border-border">
        <span class="text-text-primary">bg-surface (should be white #FFFFFF)</span>
      </div>
      
      <div class="p-4 bg-surface-hover border-2 border-border">
        <span class="text-text-primary">bg-surface-hover (should be gray-50 #F9FAFB)</span>
      </div>
      
      <div class="p-4 bg-primary text-text-inverse">
        bg-primary (should be gray-900 #111827)
      </div>
      
      <div class="p-4 bg-secondary text-text-inverse">
        bg-secondary (should be gray-600 #4B5563)
      </div>
      
      <div class="p-4 bg-accent text-text-inverse">
        bg-accent (should be gray-500 #6B7280)
      </div>
      
      <div class="p-4 border-2 border-border">
        <span class="text-text-primary">text-text-primary (should be gray-900 #111827)</span><br>
        <span class="text-text-secondary">text-text-secondary (should be gray-600 #4B5563)</span><br>
        <span class="text-text-muted">text-text-muted (should be gray-400 #9CA3AF)</span><br>
        <span class="text-link">text-link (should be gray-700 #374151)</span><br>
        <span class="text-link-hover">text-link-hover (should be black #000000)</span>
      </div>
      
      <div class="p-4 bg-black text-white">
        This div has explicit bg-black to show contrast with background
      </div>
    </div>
    
    <div class="mt-8 p-4 border-2 border-border">
      <h2 class="text-xl mb-4">Debugging Info:</h2>
      <p class="text-text-secondary">If the colors don't match the expected values, check CSS loading.</p>
      <p class="text-text-secondary">Preston Garrison should have a light theme (white background).</p>
    </div>
  </div>
