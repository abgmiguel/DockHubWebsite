# Nginx configuration for magicvideodownloader.com
# Auto-generated by generate-nginx-configs.sh
# Generated: Tue Sep  2 20:10:18 EDT 2025

server {
    listen 80;
    listen [::]:80;
    server_name magicvideodownloader.com www.magicvideodownloader.com;

    # Redirect to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name magicvideodownloader.com www.magicvideodownloader.com;

    # SSL Configuration (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/magicvideodownloader.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/magicvideodownloader.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/magicvideodownloader.com/chain.pem;
    
    # SSL Security Settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_stapling on;
    ssl_stapling_verify on;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Logging
    access_log /var/log/nginx/magicvideodownloader_access.log;
    error_log /var/log/nginx/magicvideodownloader_error.log;

    # Maximum upload size (100MB)
    client_max_body_size 100M;

    # Proxy timeouts
    proxy_connect_timeout 600;
    proxy_send_timeout 600;
    proxy_read_timeout 600;
    send_timeout 600;

    # API routes - proxy to backend container
    location /api {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Site-Database "magicvideodownloader_db";
    }

    # Serve uploads directly from nginx (more efficient than proxying)
    location /uploads {
        alias /var/www/docker/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";
        
        # Prevent directory listing
        autoindex off;
        
        # Only serve files, not directories
        try_files $uri =404;
    }

    # WebSocket support (if needed)
    location /ws {
        proxy_pass http://127.0.0.1:4321;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static assets with long cache
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|otf|eot)$ {
        proxy_pass http://127.0.0.1:4321;
        expires 30d;
        add_header Cache-Control "public, immutable";
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Main application - proxy to Astro SSR frontend
    location / {
        proxy_pass http://127.0.0.1:4321;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript 
               application/json application/javascript application/xml+rss 
               application/rss+xml application/atom+xml image/svg+xml 
               application/vnd.ms-fontobject application/x-font-ttf 
               font/opentype;
}

